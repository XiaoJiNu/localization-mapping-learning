# 贡献指南

这个仓库首先是一份个人学习记录，同时欢迎对事实错误、数学推导、代码实现和实验设计进行可复现的改进。

## 推荐的贡献方式

1. 使用对应的 Issue 模板说明问题、预期结果和验证方式；
2. 一个分支只解决一个明确问题；
3. 修改代码时同时补充或更新测试；
4. 修改数学约定时明确坐标系、变换方向、向量形式和单位；
5. 在 Pull Request 中写出你实际运行过的命令和结果。

## Markdown 排版与渲染规范

本节是全仓库 Markdown 的唯一权威规范。所有新增或修改的文档都应同时在
GitHub Preview 和 Typora 中保持可读；不能只以源码中的分隔符成对作为
“能够渲染”的证据。优先使用两者共同支持的 Markdown 写法，不把关键信息
放进依赖单个平台的 HTML 或扩展语法中。

### 公式、符号与代码标识

- 文件路径、命令、代码变量名和单独出现的帧名使用反引号，例如 `src/`、
  `make markdown`、`p_map` 和 `map`。数学变量与物理符号仍使用 LaTeX，
  不能为了规避渲染问题而把公式整体降级成代码文本。
- 短小且必须保留数学语义的表达式可使用 `$...$`。分隔符内侧不能留空格；
  起始 `$` 的左侧必须是 ASCII 空格或行首，不能紧接中文冒号、引号或顿号。
  结束 `$` 后可以接标点、空格或行尾，但不能直接连接正文。Typora 需要在
  偏好设置中启用 Inline Math 才会渲染这类表达式。
- 矩阵、推导、坐标变换链和其他复杂公式使用独立的 `$$...$$` 公式块。
  两个分隔符各自独占一行，前后保留空行，不缩进，也不放进标题、表格、
  加粗文本或链接中。
- 带前置上标、多级上下标、矩阵、求和或优化符号的表达式，即使只占一行，
  也按复杂公式处理，必须使用独立公式块。尤其不要把坐标变换符号写成行内
  `${}^{...}\mathbf{T}_{...}$`，GitHub 文件页可能直接显示源码。
- 不把独立公式块放进 `<details>` 折叠区域；GitHub 文件页可能把其中的
  `$$...$$` 直接显示为源码。含复杂公式的答案或补充说明使用普通小节。
- 公式第一次出现时说明变量、坐标系、单位和必要假设。LaTeX 花括号和
  `\begin{...}`、`\end{...}` 必须配对。
- 本仓库不使用 GitHub 文件页已拒绝的 `\operatorname*`。优化目标统一写成
  `\underset{\mathbf{x}}{\arg\min}`，并以推送后的 GitHub Preview 为最终
  渲染依据。
- 不使用旧式公式分隔符，也不把 LaTeX 命令裸写在数学环境外：

  ```markdown
  \(...\)
  \[...\]
  ```

- 面向知乎、X 等平台的文章应通过改写减少不必要的行内公式：字面帧名和
  代码标识使用反引号，数学变量仍使用 LaTeX，复杂公式改用独立公式块。
  复制发布后必须重新预览，不能假设仓库中的相对图片路径或公式会被目标
  平台自动转换。

### 强调、标题、列表与代码块

- 加粗只使用 `**内容**`。中文句末标点放在结束标记外，结束标记后若继续
  正文，应先写标点或空格，不能直接连接中文、字母或数字。
- 不跨段加粗，不把完整公式或链接包在加粗标记中。下面是推荐边界：

  ```markdown
  这是 **结论**。下一句从这里开始。
  ```

- 每个文件只使用一个一级标题，标题层级逐级递进；`#` 与标题文字之间必须
  有空格。标题、列表、公式块、图片和代码围栏前后保留空行。
- 嵌套列表统一缩进四个空格。同一组列表不要混入未缩进的另一种列表，避免
  渲染成几个互不相关的列表。
- 代码围栏必须注明语言并正确闭合。行内代码包含反引号时，使用更长的
  反引号序列包裹。

### 链接、图片与表格

- 仓库内部链接和图片使用真实、区分大小写的相对路径；提交前逐个确认目标
  存在。图片必须有能说明内容的替代文本，不能使用本机绝对路径。
- 链接文字应说明去向，资料条目优先链接教材、论文、官方文档或上游代码。
- 表格每行列数必须一致，正文中的竖线要转义。复杂公式和长段落不要放在
  表格中；面向移动端的文章优先改用短段落或列表，避免横向滚动。
- 图片只承担视觉说明，核心结论仍应在正文中出现。发布到仓库以外的平台时，
  需要重新上传图片并替换链接。

### 文件卫生与内容边界

- Markdown 文件使用 UTF-8 编码，以换行符结束；不得包含尾随空格、Tab、
  不换行空格、零宽字符或多余的 BOM。不要依赖两个行尾空格强制换行。
- 路径和代码标识使用英文，正文以中文为主。
- 将事实、引用、个人理解和待验证推断区分开。
- 不提交本地临时路径、编辑器状态或无法由他人复现的结果。

### 发布前校验

每次新增或修改 Markdown 后，至少完成以下两层检查：

1. 运行 `make markdown`；涉及代码时运行完整的 `make check`。
2. 在 Typora 和 GitHub Preview 中抽查正文、公式、加粗、列表、代码块、图片、
   链接和表格；GitHub 文件页中不得出现公式渲染错误提示，也不得原样暴露
   `${...}$`、`\operatorname*` 等源码。面向社交平台的文章还要用手机宽度预览。

自动检查可以发现未配对标记、缺失的相对目标和高风险写法，但无法证明浏览器
已成功加载数学渲染器，也无法判断远程链接是否可访问或图片在手机上是否清楚，
因此不能替代实际预览。

## 本地检查

```bash
python -m pip install -e ".[dev]"
make check
```

如果只修改文档，至少运行：

```bash
make markdown
```

## 数据和隐私

请勿提交公司代码、内部资料、真实车辆日志、非公开地图、标定结果、个人位置、密钥或其他敏感信息。公开数据也必须说明来源、许可证和获取方式；大型文件请提供下载脚本或链接，不要直接提交。

## 许可证

提交代码即表示同意按 MIT License 发布；提交到 `docs/`、`articles/`、`references/` 以及项目说明中的文档内容，按 CC BY 4.0 发布。
