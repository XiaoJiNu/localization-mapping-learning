# 调试记录：坐标变换链

## 当前结论

- [x] 从源坐标系到目标坐标系的变换方向已在代码和文档中统一。
- [x] 逐步变换与直接复合的结果在浮点误差范围内一致。
- [x] 单点使用 `(3,)`，点集使用 `(N, 3)`。

## 结果不一致时的排查顺序

1. 写出每个矩阵的源坐标系和目标坐标系，不使用含糊的 $\mathbf T_1$、
   $\mathbf T_2$。
2. 检查相邻坐标系是否能衔接，结果应为下式。

$$
{}^{\mathrm{map}}\mathbf T_{\mathrm{odom}}
{}^{\mathrm{odom}}\mathbf T_{\mathrm{base}}
= {}^{\mathrm{map}}\mathbf T_{\mathrm{base}}
$$

3. 检查代码是否采用列向量语义；本仓库接口中的点集虽按行存储，但内部保持
   与列向量公式等价的计算。
4. 检查旋转矩阵是否正交且行列式为 $+1$。
5. 分别打印旋转项、平移项和一个容易手算的测试点。

## 后续实验

- 加入错误乘法顺序，观察数值差异并解释其几何意义。
- 使用随机刚体变换进行 100 次组合测试。
- 将同一变换链写成 ROS 2 TF 的父子坐标系示例。
